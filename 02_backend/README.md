- create virtual env
  > python -m venv .venv
- install packages
  > pip install -r requirements.txt
- migration

  > flask db init

  > flask db migrate -m "msg"

  > flask db upgrade

- run
  > flask run
